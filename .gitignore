.next
node_modules
.env
.env.local
.env.*.local
out
tsconfig.tsbuildinfo
.vercel/
.turbo/

# Claude Code local state
.claude/agents/
.claude/worktrees/
.claude/settings.local.json

# MCP local config (may carry env)
.mcp.json

# Test / QA artifacts
# NOTE: __tests__/ui-sweep/**/*.png are NOT gitignored — they are committed baselines (XOL-167)
screenshots/
test-results/
playwright-report/
playwright-junit/
playwright-failures/
.playwright-mcp/

# Auth state — contains session tokens, never commit
__tests__/.auth/

# Local test credentials — never commit
.env.test
