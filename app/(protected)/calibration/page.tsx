import CalibrationDashboard from '../../../components/CalibrationDashboard';

export const metadata = {
  title: 'Calibration — xolto Admin',
};

export default function CalibrationPage() {
  return <CalibrationDashboard />;
}
